pipeline {
  agent {
    docker { image 'node:6' }
  }
  environment {
    HOME = '.'
  }
  stages {
    stage('build') {
      steps {
      }
    }
    stage('test') {
      steps {
      }
    }
  }
}
